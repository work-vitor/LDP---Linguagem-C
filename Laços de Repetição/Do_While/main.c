#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int i=0;
	do{
		i+=1;
		printf("\nWhile True e foda se!");
		printf("\nI = %d",i);
	}while(i<10);
	return 0;
}
